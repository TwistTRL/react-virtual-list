{
  "name": "react-virtual-list",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react-vertical-scrollbar": "github:twisttrl/react-vertical-scrollbar"
  },
  "peerDependencies": {
    "proptypes": "^1.1.0",
    "react": "^16.8.6"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "dist": "export NODE_ENV=production && rm -rf dist && mkdir dist && npx babel src/lib --out-dir dist --copy-files",
    "test": "react-scripts build",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "proptypes": "^1.1.0",
    "react": "^16.8.6",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-react": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "gh-pages": "^2.0.1",
    "react-dom": "^16.8.6",
    "react-scripts": "^2.1.8"
  },
  "main": "dist/index.js",
  "module": "dist/index.js",
  "babel": {
    "presets": [
      "react",
      "env",
      "stage-0"
    ]
  },
  "homepage": "https://twisttrl.github.io/rreact-virtual-list"
}
